The description leads you to believe it's blind sql injection,
but it's not.

The css file used is style.css. I never learned css so I don't know what
`fontpass123` is, but it looked suspicious and sure enough, it was the
password.

{i_l00ked_everywhere}
